language: scala
scala: 2.12.1
jdk: oraclejdk8

branches:
  only:
    - master

notifications:
    email:
      recipients:
        - kpritam@thoughtworks.com

install:
  - sbt ++$TRAVIS_SCALA_VERSION compile test scalastyle

script:
  - sbt ++$TRAVIS_SCALA_VERSION package

