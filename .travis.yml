language: scala

scala: 2.12.1

jdk: oraclejdk8

cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sbt"
  - "$HOME/.ivy2"

install:
- sbt ++$TRAVIS_SCALA_VERSION ";test;coverageReport"

deploy:
  provider: pages
  skip_cleanup: true
  github_token: "5469b9c8e0e05ed841ba009648dd0ce14eeacadb"
  local_dir: "csw-location/target/test-reports"