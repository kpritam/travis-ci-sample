language: scala

scala: 2.12.1

sudo: required

jdk: oraclejdk8

cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sbt"
  - "$HOME/.ivy2"

install:
- sbt ++$TRAVIS_SCALA_VERSION clean scalastyle compile test